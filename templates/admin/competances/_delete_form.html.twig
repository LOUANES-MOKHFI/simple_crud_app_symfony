<form method="post" action="{{ path('app_competances_delete', {'id': competance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competance.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
